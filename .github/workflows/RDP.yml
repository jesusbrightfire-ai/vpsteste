name: RDP

on:
  workflow_dispatch:

jobs:
  create-rdp-vps:
    runs-on: windows-latest
    timeout-minutes: 3600

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Enable RDP
        shell: pwsh
        run: |
          Write-Host "==> Enabling RDP..."
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name fDenyTSConnections -Value 0
          netsh advfirewall firewall add rule name="RDP" dir=in action=allow protocol=TCP localport=3389
          Write-Host "✅ RDP Enabled!"

      - name: Create RDP user (no password)
        shell: pwsh
        run: |
          Write-Host "==> Creating user without password..."
          $user = "RDP"
          if (-not (Get-LocalUser -Name $user -ErrorAction SilentlyContinue)) {
            net user $user /add /y
          }
          net localgroup "Administrators" $user /add
          net localgroup "Remote Desktop Users" $user /add
          reg add "HKLM\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Winlogon" /v "AutoAdminLogon" /t REG_SZ /d "1" /f
          reg add "HKLM\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Winlogon" /v "DefaultUserName" /t REG_SZ /d "$user" /f
          reg add "HKLM\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Winlogon" /v "DefaultPassword" /t REG_SZ /d "" /f
          reg add "HKLM\SYSTEM\CurrentControlSet\Control\Lsa" /v "LimitBlankPasswordUse" /t REG_DWORD /d 0 /f
          "RDP_USER=$user" | Out-File -FilePath $env:GITHUB_ENV -Append
          Write-Host "✅ User created: $user (no password, auto login enabled)"

      - name: Install Tailscale
        shell: pwsh
        run: |
          Write-Host "==> Installing Tailscale..."
          $url = "https://pkgs.tailscale.com/stable/tailscale-setup-1.82.0-amd64.msi"
          $file = "$env:TEMP\tailscale.msi"
          Invoke-WebRequest -Uri $url -OutFile $file
          Start-Process msiexec.exe -ArgumentList "/i `"$file`" /quiet /norestart" -Wait
          Write-Host "✅ Installed."

      - name: Connect to Tailscale
        shell: pwsh
        run: |
          & "$env:ProgramFiles\Tailscale\tailscaled.exe" start-service
          Start-Sleep -Seconds 5
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname="gh-runner-${{ github.run_id }}"
          $ip = (& "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4) | Select-Object -First 1
          if (-not $ip) { Write-Error "Failed to obtain Tailscale IP"; exit 1 }
          "TAILSCALE_IP=$ip" | Out-File -FilePath $env:GITHUB_ENV -Append
          Write-Host "✅ Tailscale IP: $ip"

      - name: Install 7-Zip
        shell: pwsh
        run: |
          Write-Host "==> Installing 7-Zip..."
          $url = "https://www.7-zip.org/a/7z2301-x64.msi"
          $file = "$env:TEMP\7zip.msi"
          Invoke-WebRequest -Uri $url -OutFile $file
          Start-Process msiexec.exe -ArgumentList "/i `"$file`" /quiet /norestart" -Wait
          Write-Host "✅ 7-Zip Installed."

      - name: Download and extract files to Public Desktop
        shell: pwsh
        run: |
          $publicDesktop = "C:\Users\Public\Desktop"
          if (-not (Test-Path $publicDesktop)) { New-Item -ItemType Directory -Path $publicDesktop -Force }

          $url = "https://www.dropbox.com/scl/fi/qy9evq8pj3fyvq4e77j8c/arquivos_extraidos.zip?rlkey=762hqioz27j68i26pgfi4cr6d&st=pl5dltbr&dl=1"
          $zipPath = "$env:TEMP\arquivos_extraidos.zip"

          Write-Host "==> Downloading files..."
          Invoke-WebRequest -Uri $url -OutFile $zipPath -UseBasicParsing
          Start-Sleep -Seconds 2

          Write-Host "==> Extracting files to Public Desktop..."
          $sevenZip = "${env:ProgramFiles}\7-Zip\7z.exe"
          & $sevenZip x $zipPath -o"$publicDesktop" -y

          Write-Host "✅ Files extracted to Public Desktop!"

      - name: Show connection info
        shell: pwsh
        run: |
          Write-Host "`n==============================="
          Write-Host "🟢 SUA VPS ESTÁ ONLINE!"
          Write-Host "🔹 IP: $env:TAILSCALE_IP"
          Write-Host "🔹 Usuário: $env:RDP_USER"
          Write-Host "🔹 Senha: nenhuma"
          Write-Host "🔹 UTILIZE POR 6 HORAS SEM PREOCUPAÇÃO!"
          Write-Host "==============================="

      - name: Keep Alive
        shell: pwsh
        run: |
          Write-Host "⏳ Mantendo VPS ativa..."
          Start-Sleep -Seconds 3600
